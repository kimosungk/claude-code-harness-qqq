# Project-local Claude Code state — generated by scripts/install-qqq-hooks.sh
# (dogfooding the plugin on its own repo). Each contributor runs install on
# their checkout; nothing under .claude/ should be tracked.
.claude/
__pycache__/
*.pyc
.codex
.DS_Store
