{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Phase 2 Gate 2 (Architect) Verdict",
  "description": "Structured Codex output for one phase-2 architect round. Engine, mode, round, plan fingerprint, and invalidated-by metadata are added by the persist step. Section bodies are markdown strings rendered verbatim under their respective headers.",
  "type": "object",
  "required": ["verdict", "blocking_reasons", "next_action", "sections"],
  "additionalProperties": false,
  "properties": {
    "verdict": { "enum": ["OKAY", "REJECT"] },
    "blocking_reasons": {
      "type": "array",
      "items": { "type": "string" },
      "description": "One-line blockers keyed to file:line or exact file path. Empty when verdict is OKAY."
    },
    "next_action": {
      "type": "string",
      "description": "One-line planner action."
    },
    "sections": {
      "type": "object",
      "required": [
        "structure_fit",
        "reuse_and_boundaries",
        "contracts_and_security",
        "simulated_steps",
        "evidence"
      ],
      "additionalProperties": false,
      "properties": {
        "structure_fit": {
          "type": "string",
          "description": "Markdown body for ## Structure Fit."
        },
        "reuse_and_boundaries": {
          "type": "string",
          "description": "Markdown body for ## Reuse and Boundaries."
        },
        "contracts_and_security": {
          "type": "string",
          "description": "Markdown body for ## Contracts and Security."
        },
        "simulated_steps": {
          "type": "string",
          "description": "Markdown body for ## Simulated Steps."
        },
        "evidence": {
          "type": "string",
          "description": "Markdown body for ## Evidence — key file:line anchors."
        }
      }
    }
  }
}
