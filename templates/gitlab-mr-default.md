# qqq default GitLab MR template

> Reference template used by `dev-mr-create` when the target repo has no
> `.gitlab/merge_request_templates/*.md`. **Not** copied into the project —
> the workflow injects it via `glab mr create --description @<tmpfile>`.

## Changes

<!-- One-line summary per logical change. Link issues with `Closes #N`. -->
-

## Context

<!-- Why this change is needed. Background a reviewer needs but that isn't
     obvious from the diff. Link to the requirement / design doc if any. -->

## Test plan

<!-- How the reviewer can verify. Automated steps first, manual second. -->
- [ ]
- [ ]

## Rollback

<!-- How to revert if this ships and breaks something. Often `git revert`,
     but note DB migrations / feature flags / config changes separately. -->

## Screenshots

<!-- UI changes only. Before / after pairs preferred. Drop / or delete this
     section if the MR has no user-visible surface. -->
